# Query Expansion Profile Configuration
# This profile enables multi-query retrieval by expanding the user's question into multiple perspectives.

spring:
  ai:
    vectorstore:
      pgvector:
        index-type: HNSW
        distance-type: COSINE_DISTANCE
        dimensions: 1024
        table-name: vector_store
        initialize-schema: on
